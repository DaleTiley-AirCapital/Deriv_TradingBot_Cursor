Implement the following fixes now.

## 1. Fix the confirmed strategy-name bug
The Trend Continuation family is emitting `trend-continuation` but the settings/strategy whitelist still uses `trend-pullback`.

Make them consistent across:
- emitted signal strategy name
- enabled strategies list
- UI strategy list
- monthly optimizer naming
- any DB persisted strategy identifiers

Then verify with one example signal that a trend-continuation/trend-pullback trade now survives the router.

## 2. Add active symbol validation at startup and before subscribing
Do not assume symbol codes are valid.

Use Deriv Active Symbols / market-data discovery to:
- fetch all active underlyings available to this account
- build a validated symbol map
- mark configured symbols as:
  - valid
  - invalid
  - unavailable for this account
- refuse to subscribe to invalid symbols
- surface this status in logs and UI

## 3. Audit and fix the configured symbol codes
Specifically verify:
- BOOM300
- CRASH300
- BOOM200
- CRASH200
- STPIDX

Return:
- configured symbol
- whether it exists in Active Symbols
- actual API symbol if different
- subscription status
- reason if not streaming

## 4. Add a symbol diagnostics panel in the UI
For every configured symbol show:
- configured code
- instrument family
- active symbol found? yes/no
- live stream connected? yes/no
- last tick timestamp
- last tick value
- tick count in last 5 minutes
- error/rejection reason

## 5. Add hard startup warnings
If a symbol is invalid or unavailable:
- log loudly
- show in UI
- exclude from scheduler automatically

## 6. Add a stream health watchdog
Per symbol:
- if no tick received within threshold, mark stream stale
- auto-resubscribe
- log failures
- surface stale state in UI

## 7. Fix UI/settings mismatches
Implement immediately:
- remove macro-bias from strategy list
- fix trend-pullback / trend-continuation mismatch
- add extraction settings to UI
- add profit-harvesting settings to UI
- add correlated family cap setting to UI
- implement max drawdown if it is shown in the UI
- if a setting is shown but not wired, either wire it or remove it

## 8. Return proof
After implementing, return:
- exact files changed
- exact functions changed
- one sample active-symbol response mapping for the affected symbols
- one sample BOOM300 or CRASH300 live tick log
- one sample invalid symbol rejection log if BOOM200/CRASH200 are unsupported