Verify whether Task #86 is fully complete against the current codebase.

Do not make changes yet.
Only inspect and report.

I need a strict verification of the CURRENT filesystem and imports, not the task description.

Check all of the following and return PASS/FAIL for each:

1. Directory structure
- Does `artifacts/api-server/src/lib/` still exist?
- Do these directories now exist?
  - `artifacts/api-server/src/core/`
  - `artifacts/api-server/src/infrastructure/`
  - `artifacts/api-server/src/runtimes/`

2. File locations
Report the exact current path of:
- features.ts
- regimeEngine.ts
- scoring.ts
- signalRouter.ts
- strategies.ts
- tradeEngine.ts
- extractionEngine.ts
- pendingSignals.ts
- deriv.ts
- openai.ts
- scheduler.ts
- symbolValidator.ts
- candleExport.ts
- backtestEngine.ts
- version.ts

3. Old import paths
Run and report exact output for:
- `grep -r "../lib/" artifacts/api-server/src/`
- `grep -r "./lib/" artifacts/api-server/src/`

4. Route imports
Verify all route files under `artifacts/api-server/src/routes/` no longer import from `../lib/` and instead import from the correct new location.

5. index.ts imports
Verify `artifacts/api-server/src/index.ts` no longer imports from `./lib/`.

6. No fallback/shim paths
Confirm whether any compatibility re-exports, shim files, wrapper files, aliases, or temporary bridge files were left behind after the move.

7. TypeScript validation
Run:
- `pnpm tsc --noEmit`
Report whether it passes with zero errors.

8. Runtime startup
Confirm whether the API server starts cleanly with no module resolution errors.

9. Final verdict
Return:
- PASS if Task #86 is fully complete
- PARTIAL if structure is partly done but old paths/shims/issues remain
- FAIL if the repo is still materially in the old structure

Output format:
- short summary
- checklist with PASS/FAIL per section
- exact remaining problems, if any
- exact files/imports/paths still needing correction