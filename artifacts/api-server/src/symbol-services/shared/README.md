# Symbol Services Shared Contract Layer

This folder defines shared contracts and runtime-flow helpers for symbol services.

Milestone 1 scope:

- Contracts and type shapes are introduced.
- Registry and scaffold services are introduced.
- No live/backtest behavior cutover is performed yet.

The active runtime scheduler/backtest paths remain unchanged until later milestones.